import 'package:flutter/material.dart';

class DetailAccounts extends StatefulWidget {
  const DetailAccounts({Key? key}) : super(key: key);

  @override
  State<DetailAccounts> createState() => _DetailAccountsState();
}

class _DetailAccountsState extends State<DetailAccounts> {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Row(
          children: [
            Column(
              children: const [
                
              ],
            ),
          ],
        )),
    );
  }
}
